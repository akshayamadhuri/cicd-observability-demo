name: CI Pipeline with OTel Tracing

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  OTEL_ENDPOINT: ${{ secrets.OTEL_EXPORTER_ENDPOINT }}
  OTEL_AUTH: ${{ secrets.OTEL_AUTH_HEADER }}
  SERVICE_NAME: "cicd-observability-demo"

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # ── Step 1: Install otel-cli ──────────────────────────────────
      - name: Install otel-cli
        run: |
          DOWNLOAD_URL=$(curl -s https://api.github.com/repos/equinix-labs/otel-cli/releases/latest \
            | grep "browser_download_url.*linux_amd64.tar.gz" \
            | cut -d '"' -f 4)
          echo "Downloading from: $DOWNLOAD_URL"
          curl -sL "$DOWNLOAD_URL" -o otel-cli.tar.gz
          tar -xzf otel-cli.tar.gz
          chmod +x otel-cli
          sudo mv otel-cli /usr/local/bin/
          which otel-cli

      # ── Step 2: Checkout code ─────────────────────────────────────
      - name: Checkout code
        uses: actions/checkout@v4

      # ── Step 3: Setup Python ──────────────────────────────────────
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      # ── Step 4: Create root span + save TRACEPARENT ───────────────
      - name: Create root pipeline trace
        run: |
          eval $(otel-cli span \
            --endpoint "${OTEL_ENDPOINT}/v1/traces" \
            --otlp-headers "Authorization=${OTEL_AUTH}" \
            --service "${SERVICE_NAME}" \
            --name "pipeline-run" \
            --attrs "github.run_id=${{ github.run_id }},github.sha=${{ github.sha }},github.actor=${{ github.actor }},github.ref=${{ github.ref }}" \
            --tp-export)
          echo "TRACEPARENT=${TRACEPARENT}" >> $GITHUB_ENV

      # ── Step 5: Trace checkout ────────────────────────────────────
      - name: Trace - checkout
        run: |
          otel-cli span \
            --endpoint "${OTEL_ENDPOINT}/v1/traces" \
            --otlp-headers "Authorization=${OTEL_AUTH}" \
            --service "${SERVICE_NAME}" \
            --name "step/checkout" \
            --attrs "step.name=checkout,github.run_id=${{ github.run_id }}"

      # ── Step 6: Install dependencies (timed) ─────────────────────
      - name: Install dependencies
        run: |
          START=$(date +%s%N)
          pip install -r requirements.txt
          END=$(date +%s%N)
          DURATION=$(( (END - START) / 1000000 ))

          otel-cli span \
            --endpoint "${OTEL_ENDPOINT}/v1/traces" \
            --otlp-headers "Authorization=${OTEL_AUTH}" \
            --service "${SERVICE_NAME}" \
            --name "step/install-dependencies" \
            --attrs "step.name=install,duration_ms=${DURATION},github.run_id=${{ github.run_id }}"

      # ── Step 7: Run tests (timed + capture pass/fail) ─────────────
      - name: Run tests
        run: |
          START=$(date +%s%N)
          set +e
          pytest test_app.py -v --tb=short 2>&1 | tee test-output.txt
          TEST_EXIT_CODE=$?
          set -e
          END=$(date +%s%N)
          DURATION=$(( (END - START) / 1000000 ))

          TESTS_PASSED=$(grep -c "PASSED" test-output.txt || echo 0)
          TESTS_FAILED=$(grep -c "FAILED" test-output.txt || echo 0)

          otel-cli span \
            --endpoint "${OTEL_ENDPOINT}/v1/traces" \
            --otlp-headers "Authorization=${OTEL_AUTH}" \
            --service "${SERVICE_NAME}" \
            --name "step/run-tests" \
            --attrs "step.name=test,duration_ms=${DURATION},tests.passed=${TESTS_PASSED},tests.failed=${TESTS_FAILED},exit_code=${TEST_EXIT_CODE},github.run_id=${{ github.run_id }}" \
            --status-code $([ $TEST_EXIT_CODE -eq 0 ] && echo "ok" || echo "error")

          exit $TEST_EXIT_CODE

      # ── Step 8: Build artifact ────────────────────────────────────
      - name: Build artifact
        run: |
          START=$(date +%s%N)
          mkdir -p dist
          cp app.py dist/
          zip -r dist/app.zip dist/app.py
          END=$(date +%s%N)
          DURATION=$(( (END - START) / 1000000 ))

          otel-cli span \
            --endpoint "${OTEL_ENDPOINT}/v1/traces" \
            --otlp-headers "Authorization=${OTEL_AUTH}" \
            --service "${SERVICE_NAME}" \
            --name "step/build-artifact" \
            --attrs "step.name=build,duration_ms=${DURATION},github.run_id=${{ github.run_id }}"

      # ── Step 9: Close root span (always runs) ─────────────────────
      - name: Close pipeline trace
        if: always()
        run: |
          STATUS=$([ "${{ job.status }}" == "success" ] && echo "ok" || echo "error")

          otel-cli span \
            --endpoint "${OTEL_ENDPOINT}/v1/traces" \
            --otlp-headers "Authorization=${OTEL_AUTH}" \
            --service "${SERVICE_NAME}" \
            --name "pipeline-complete" \
            --attrs "pipeline.status=${{ job.status }},github.run_id=${{ github.run_id }}" \
            --status-code "${STATUS}"
